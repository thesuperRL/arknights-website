# Heroku Postgres (or any PostgreSQL) - when set, accounts use SQL instead of JSON
# Get this from Heroku: heroku config:get DATABASE_URL (or add Heroku Postgres add-on)
# DATABASE_URL=postgres://user:password@host:5432/database?sslmode=require

# Azure SQL Database Configuration (for niches/migration scripts)
AZURE_SQL_SERVER=your-server.database.windows.net
AZURE_SQL_DATABASE=your-database-name
AZURE_SQL_USER=your-username
AZURE_SQL_PASSWORD=your-password
AZURE_SQL_PORT=1433

# Alternative: Use connection string instead of individual settings
# AZURE_SQL_CONNECTION_STRING=Server=your-server.database.windows.net;Database=your-database;User Id=your-user;Password=your-password;Encrypt=true

# Server Configuration
PORT=3000
NODE_ENV=development

# Upload script configuration (optional)
# CLEAR_EXISTING=true
